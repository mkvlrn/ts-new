{
  "name": "ts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "test:cov": "rm -rf coverage && vitest run --coverage"
  },
  "dependencies": {},
  "devDependencies": {}
}
